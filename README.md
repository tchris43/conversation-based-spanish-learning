# conversation-based-spanish-learning
This project is a multi-agent system that uses natural Spanish conversation to teach the user Spanish. It keeps track of the vocab that the user has learned and uses an algorithm to control a spaced review. The words that need to be reviewed get injected into the context each call so the LLM knows to use them in conversation.
